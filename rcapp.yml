apiVersion: v1
kind: ReplicationController
metadata:
  name: mytomcat-rc
spec:
  replicas: 2
  selector:
    app: mytomcat
    version: 2.7.01
  template:
    metadata:
      labels:
        app: mytomcat
        version: 2.7.01
    spec:
      containers:
      - name: mytomcat-pod
        image: ravidevops26/ravilogin:1
        ports:
        - containerPort: 8080